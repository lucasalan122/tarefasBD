##banco de dados
CREATE DATABASE tarefas;

CREATE TABLE tarefa1(
    id INT UNSIGNED AUTO_INCREMENT PRIMARY KEY NOT NULL,
    concluida VARCHAR(10) NOT NULL,
    nome_tarefa VARCHAR(20) NOT NULL
     
);

